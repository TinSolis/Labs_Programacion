table: silver.customers
purpose: Clientes con customer_id y customer_unique_id sin agrupar.
grain: Una fila por customer_id.
primary_key: [customer_id]
owner: Equipo Pudubella
lineage: [bronze.customers]
classification: interno
required_columns:
  - customer_id
  - customer_unique_id
column_types:
  customer_id: string
  customer_unique_id: string
allow_empty: false
constraints:
  customer_id:
    nullable: false
    unique: true
  customer_unique_id:
    nullable: false
